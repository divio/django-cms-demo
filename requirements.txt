Django==1.7.8
django-cms==3.1.0
django-reversion==1.8.5             # not a part of django-cms core
djangocms-text-ckeditor==2.5.1      # not a part of django-cms core
psycopg2==2.6                       # required for django.db.backends.postgresql_psycopg2

# additional addons
# we always want to use the bleeding-edge version in order to test if releases break
aldryn-bootstrap3                   # adds django-filer
