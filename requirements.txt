Django==1.7.8                       # rq.filter: >=1.6,<1.8
django-cms==3.1.0
django-reversion==1.8.7             # not a part of django-cms core
djangocms-text-ckeditor==2.5.2      # not a part of django-cms core
psycopg2==2.6                       # required for django.db.backends.postgresql_psycopg2

# additional addons
# use the bleeding-edge version for testing purposes
aldryn-bootstrap3                   # adds django-filer
